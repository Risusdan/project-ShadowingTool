# Phase 2 — Playback Controls & Transcript Sync

## Steps
- [x] Set up frontend test infrastructure (vitest, testing-library, jsdom)
- [x] Implement `findActiveSegmentIndex` utility + tests (6 tests)
- [x] Implement `usePlayerSync` hook + tests (5 tests)
- [x] Implement `usePlaybackControls` hook + tests (6 tests)
- [x] Implement `PlaybackControls` component + tests (7 tests)
- [x] Update `TranscriptPanel` — highlighting, auto-scroll, loop range, shift+click (6 tests)
- [x] Wire everything in `App.tsx` + update `VideoPlayer.tsx`
- [x] Verify: all tests pass (30/30), tsc clean, vite build clean

## Review
- 30 tests across 5 test files, all passing
- Zero TypeScript errors
- Clean production build
- New files: 10 created, 5 modified
